# Immersive room builders

`build_studio_rooms.py` generates the original Practice, Recording, Production and Mix room `.blend`, preview and GLB artifacts. `validate_studio_rooms.py` reimports every GLB and verifies its required semantic hero nodes and measured geometry.

The scripts require Blender 5.x and no external assets or textures.
